<div class="comment clearfix">
  <div class="commentCont">
    <p class="commentName"><strong><%= @post.user.username %></strong></p>
    <p class="commentBody"><%= @post.comments %></p>
    <!--<p class="commentTime"><%# time_ago_in_words(comment.created_at) %> Ago</p>-->
  </div>

  <p><%= link_to 'Delete', [comment.post, comment], method: :delete, class: 'button', data: { confirm: 'Are you sure?' } %></p>
</div>